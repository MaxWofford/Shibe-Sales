- if user_signed_in?
  / If the user is signed in, a sign-out link is in the header
  %h2
    Welcome, #{current_user.email}
  = link_to "Update Account", edit_user_registration_path, class: "button"
- else
  %p
    = link_to "Log in", new_user_session_path, class: "button"
    = link_to "Sign up", new_user_registration_path, class: "button"