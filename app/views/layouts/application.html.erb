<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<title>
			<%= content_for?(:title) ? yield(:title) : "Shibe Sales" %>
		</title><%= stylesheet_link_tag    "application" %><%= javascript_include_tag "vendor/modernizr" %><%= csrf_meta_tags %>
	</head>
	<body>
		<div class="row">
			<div id="header" class="row">
				<div class="row">
					<div class="large-12 columns">
						<nav class="top-bar data-topbar">
							<ul class="title-area">
								<li class="name">
									<h1>
										<%= link_to "Shibe Sales", root_path %>
									</h1>
								</li>
								<li class="toggle-topbar">
									<span>Menu</span>
								</li>
							</ul>
							<section class="top-bar-section">
								<ul class="right">
									<li>
										<%= link_to "FAQ", faq_path %>
									</li>
									<li>
										<%= link_to "Contact", contact_path %>
									</li>
									<li>
										<%= link_to "Recommend", recommend_path %>
									</li><% if user_signed_in? %>
									<li>
										<%= link_to "Log out", destroy_user_session_path, method: :delete %>
									</li><% end %>
								</ul>
							</section>
						</nav>
					</div>
				</div>
			</div>
			<% if current_page?(admin_path)%>
			<div class="large-10 columns">
				<% flash.each do |name, msg| %><%= content_tag(:div, msg, class: "alert alert-info") %><% end %>
				<%= yield %>
			</div>
			<% else %>
			<div class="row">
				<div id="sidePanel">
					<div class="large-2 small-12 columns">
						<div class="panel">
							<h3>
								Miss a steam sale?
							</h3>
							<h5 class="subheader">
								Buy the game on sale with <a href="http://dogecoin.com/">Ðogecoin</a>!
							</h5>
							<div class="horizontal-line"></div>
						</div>
						<div class="hide-for-small panel">
							<div class="panel">
								<h6>
									Powered by: <a href="https://www.dogeapi.com/">ƉogeAPI</a>
								</h6>
								<div class="horizontal-line"></div>
							</div>
						</div>
					</div>
				</div>
				<div class="large-10 columns">
					<% flash.each do |name, msg| %><%= content_tag(:div, msg, class: "alert alert-info") %><% end %>
					<%= yield %>
				</div>
			</div>
			<% end %>
			<footer id="footer" class="row no-border">
				<div class="large-12 columns">
					<div class="row footer" style="font-size: .25em;">
						<div class="horizontal-line"></div>
						<div class="large-6 columns">
							<p>
								© <a href="">ShibeSales</a> 2014
							</p>
						</div>
						<div class="large-6 columns">
							<ul class="inline-list right">
								<% if current_page?(admin_path)%>
								<% else %>
								<li>
									<%= link_to "Admin Page", admin_path %>
								</li>
								<% end %>
							</ul>
						</div>
					</div>
				</div>
			</footer><%= javascript_include_tag "application" %>
		</div>
	</body>
</html>